# WISE v0.90.3 Diagnostic Subfield RA 57.9 Dec 19.9

Date: 2026-06-30 UTC

## Purpose

Run the final remaining distinct support-positive WISE/NEOWISE diagnostic
subfield selected by the v0.90.3 scale plan after the rank 1 and rank 2
subfields produced only adversarial `REJECT` verdicts. This records the
operator-provided run output and the follow-up offline adversarial review so
future agents do not ask for the same command again.

## Command

```bash
# Pull latest merged main before running any operator workflow.
git pull origin main

# Keep native numerical libraries to one thread each so local Python pipeline
# work remains deterministic and avoids oversubscription.
export OMP_NUM_THREADS=1
export OPENBLAS_NUM_THREADS=1
export VECLIB_MAXIMUM_THREADS=1
export NUMEXPR_MAX_THREADS=1
export PYTHONPATH=src

# Run the next remaining support-positive WISE diagnostic subfield. This is
# dry-run only, prevents macOS sleep, writes full ScoredNEO review packets when
# tracklets are produced, and does not enable external submission.
caffeinate -i uv run --python 3.14 python Skills/run_pipeline.py \
  --ra 57.9 --dec 19.9 --radius 0.0466 \
  --start-jd 2458880.5 --end-jd 2459250.5 \
  --surveys WISE --no-resume --force-refresh \
  --review-packet-out Logs/reports/wise_diag_subfield_57p9_19p9_review_packets.json \
  --output Logs/reports/wise_diag_subfield_57p9_19p9_candidates.json
```

## Pipeline Results

- Run ID: `e7ee52875c25`
- Mode: dry run; no external submission
- Field: RA `57.9`, Dec `19.9`, radius `0.0466` degrees
- Window: JD `2458880.5` to `2459250.5`
- Survey: WISE
- WISE rows/alerts: `668`
- Preprocessed sources: `665/668`
- Detected singleton candidates: `665`
- Known matches: `0`
- Link seed pairs: `18776`
- Tracklets formed: `2`
- Candidates processed: `2`
- Submission-ready candidates: `0`
- Review-packet file: `Logs/reports/wise_diag_subfield_57p9_19p9_review_packets.json`
- Review packets written: `2`
- Candidate-output file: `Logs/reports/wise_diag_subfield_57p9_19p9_candidates.json`

## Candidate Summary

| Object ID | NEO probability | Artifact probability | Known-object probability | Arc days | Observations | Alert pathway |
|---|---:|---:|---:|---:|---:|---|
| `dfabc4a9-940e-463e-a8d2-883f20cbea6a` | `0.08%` | `98.21%` | `0.93%` | `201.85` | `3` | `internal_candidate` |
| `af515bf7-6636-4294-ab3e-6bbb0b21a6cb` | `0.09%` | `98.81%` | `0.67%` | `159.26` | `3` | `internal_candidate` |

## Offline Adversarial Review

```bash
# Review only after the pipeline reported a non-zero full ScoredNEO packet
# count. This offline mode performs no external service calls.
PYTHONPATH=src uv run --python 3.14 python Skills/adversarial_review.py \
  Logs/reports/wise_diag_subfield_57p9_19p9_review_packets.json \
  --offline --json \
  > Logs/reports/wise_diag_subfield_57p9_19p9_adversarial_review.json
```

The adversarial review returned exit code `1`, which is expected for rejected
candidates. The JSON report was written locally to
`Logs/reports/wise_diag_subfield_57p9_19p9_adversarial_review.json`.

| Verdict | Count |
|---|---:|
| `REJECT` | `2` |
| `BORDERLINE` | `0` |
| `SURVIVE` | `0` |

Both candidates failed the same challenge family seen in the rank 1 and rank 2
diagnostics:

- `orbit_quality`: no orbital elements were computed.
- `real_bogus`: WISE archive packets have no ZTF-style real/bogus score.
- `artifact_posterior`: posterior artifact probability was approximately
  `0.98` to `0.99`.
- `neo_dominance`: NEO posterior was approximately `0.001`.
- `motion_rate`: measured motion was below the hard lower bound of
  `0.05 arcsec/hr`.

## Root Cause

The run did not expose a code crash. The production blocker is candidate
quality in this Taurus WISE diagnostic set: linked three-observation tracklets
are near-stationary, artifact-dominated internal candidates with no usable
orbit solution and no WISE-native real/bogus evidence, so the adversarial gate
correctly rejects them before operator review.

The operator transcript also shows the checkout prompt ending on
`codex/d1-rank-four-operator-blocker` even though operator workflows are
intended to run from `main`. The branch was a docs-only blocker note one commit
ahead of `main`, so it did not change pipeline code or scientific results, but
future operator commands must still begin from `main` after the relevant PR is
merged.

## Interpretation

The v0.90.3 Taurus WISE scale-plan loop is exhausted for practical production
purposes:

- Rank 1 RA `58.1`, Dec `19.9`: `3` packets, `3/3` `REJECT`.
- Rank 2 RA `57.9`, Dec `20.1`: `2` packets, `2/2` `REJECT`.
- Rank 3 RA `58.1`, Dec `20.1`: prior same-parameter diagnostic produced
  zero tracklets and should not be rerun.
- Rank 4 RA `57.9`, Dec `19.9`: `2` packets, `2/2` `REJECT`.

The next D1 production action should move away from this Taurus diagnostic
cell/window and select a new WISE/NEOWISE field-window strategy likely to
produce faster, non-static candidates, or improve the WISE-specific filtering
and linking criteria before another operator live run.

## Safety

No MPC submission was performed. No NASA/PDCO pathway was triggered. No impact
probability was computed or claimed. Raw run outputs remain under ignored
`Logs/`; this document is the durable sanitized evidence.
