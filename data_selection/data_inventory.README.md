# Data Inventory

`data_inventory.parquet` is required by the Astrometrics data-selection policy
once inventory tooling starts emitting machine-readable records. This repository
does not yet have that generator, so this placeholder documents the expected
location without committing a fake parquet file.

Do not create a placeholder parquet unless it is produced by real inventory
tooling and contains a schema that downstream checks can validate.
