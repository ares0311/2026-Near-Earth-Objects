# Example local storage configuration. Copy values into an untracked local env
# file or shell profile; do not commit machine-specific paths with secrets.

# Directory for large source downloads or cache files on an operator-provided
# external SSD or explicitly chosen local disk.
ASTROMETRICS_CACHE_DIR=

# Optional directory for compact exported artifacts intended for cloud sync.
ASTROMETRICS_EXPORT_DIR=
